// Create a constant variable for the Json Data

const jsonData = data;

// Create Function for Charts Sample

// function charts(sample) {

let sampleData = jsonData[0]

let employeeCount = sampleData.employees

let profit = sampleData.profit_billions

console.log(employeeCount,profit)

// }
